{
  "name": "dotctx",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": ["src"],
  "scripts": {
    "build": "npm run build -w src",
    "dotctx": "npm run start -s -w src --"
  }
}
