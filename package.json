{
  "name": "agent-skills",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": ["cli"],
  "scripts": {
    "build": "npm run build -w cli",
    "skill": "npm run start -w cli --"
  }
}
